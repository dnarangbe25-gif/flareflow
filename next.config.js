/** @type {import('next').NextConfig} */
const nextConfig = {
  // This file exists to satisfy Vercel's framework detection.
  // The actual config is in frontend/next.config.mjs
};

module.exports = nextConfig;
